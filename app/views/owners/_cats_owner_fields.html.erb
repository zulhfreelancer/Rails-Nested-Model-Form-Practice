<br/>
  <%= f.label :cat %>
  <%= f.select :cat_id, options_for_select(Cat.all.map{|c| [c.name, c.id]}, f.object.cat_id)%>
  
  <%= f.label :quantity %>
  <%= f.text_field :quantity %>

  <%= f.label :status %>
  <%= f.text_field :status %>

  <%= f.check_box :_destroy %><%= f.label :_destroy, "Remove Cat" %><br>
